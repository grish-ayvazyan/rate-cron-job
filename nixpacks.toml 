[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = [
  "python3 -m pip3 install --upgrade pip",
  "python3 -m pip3 install -r requirements.txt"
]

[phases.start]
cmd = "python3 euro_bot.py"
