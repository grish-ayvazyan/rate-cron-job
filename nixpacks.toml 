[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[phases.start]
cmd = "python3 euro_bot.py"
